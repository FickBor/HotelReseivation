<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <include layout="@layout/include_add_top_bar" />

    <AutoCompleteTextView
        android:id="@+id/auto_buquan"
        android:hint="请输入文字进行搜索"
        android:completionThreshold="1"
        android:paddingLeft="10dp"
        android:layout_width="match_parent"
        android:layout_height="@dimen/base_edit_height"
        android:layout_margin="10dp"
        android:background="@drawable/input"/>

    <Spinner
        android:id="@+id/sp_manyi"
        android:layout_width="match_parent"
        android:layout_height="@dimen/base_edit_height"
        android:paddingLeft="10dp"
        android:layout_margin="10dp"
        android:entries="@array/sp"
        android:maxLength="11"
        android:background="@drawable/input"></Spinner>

    <!--<EditText -->
    <!--android:id="@+id/edit_manyi"-->
    <!--android:layout_width="match_parent"-->
    <!--android:layout_height="@dimen/base_edit_height"-->
    <!--android:paddingLeft="10dp"-->
    <!--android:layout_margin="10dp"-->
    <!--android:inputType="phone"-->
    <!--android:maxLength="11"-->
    <!--android:hint="满意程度"-->
    <!--android:background="@drawable/input"-->
    <!--/>-->

    <EditText
        android:id="@+id/edit_describe"
        android:hint="@string/describe_hint"
        android:background="@drawable/input"
        android:paddingLeft="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:minLines="4"
        />
</LinearLayout>